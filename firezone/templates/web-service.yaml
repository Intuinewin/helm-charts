apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" (include "firezone.fullname" $) "web" }}
  labels:
    {{- include "firezone.web.labels" . | nindent 4 }}
    {{ .Values.global.erlangCluster.kubernetes.selector.key }}: {{ .Values.global.erlangCluster.kubernetes.selector.value }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.global.erlangCluster.epmdPort }}
      targetPort: epmd
      protocol: TCP
      name: epmd
    - port: {{ .Values.global.erlangCluster.distributionPort }}
      targetPort: distribution
      protocol: TCP
      name: distribution
  selector:
    {{- include "firezone.web.selectorLabels" . | nindent 4 }}
